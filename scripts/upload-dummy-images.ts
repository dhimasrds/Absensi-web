import { createClient } from '@supabase/supabase-js'
import * as dotenv from 'dotenv'
import { readFileSync } from 'fs'
import { join } from 'path'

dotenv.config({ path: '.env.local' })

const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL!
const supabaseKey = process.env.SUPABASE_SERVICE_ROLE_KEY!

if (!supabaseUrl || !supabaseKey) {
  console.error('Missing Supabase credentials')
  process.exit(1)
}

const supabase = createClient(supabaseUrl, supabaseKey)

async function uploadDummyImages() {
  console.log('📸 Uploading dummy attendance proof images...\n')

  // Create a proper minimal JPEG image (red 10x10 pixel square)
  // This is a complete, valid JPEG that browsers can render
  const dummyImageBuffer = Buffer.from([
    
  ])

  // Get all attendance records with proof_image_path
  const { data: attendances, error: fetchError } = await supabase
    .from('attendance_logs')
    .select('id, proof_image_path, proof_image_mime')
    .not('proof_image_path', 'is', null)

  if (fetchError) {
    console.error('❌ Error fetching attendance records:', fetchError)
    return
  }

  if (!attendances || attendances.length === 0) {
    console.log('ℹ️  No attendance records with proof images found')
    return
  }

  console.log(`Found ${attendances.length} records with proof images\n`)

  let uploaded = 0
  let skipped = 0
  let failed = 0

  for (const attendance of attendances) {
    const path = attendance.proof_image_path

    // Check if file already exists
    const { data: exists } = await supabase.storage
      .from('attendance-proofs')
      .list(path.split('/').slice(0, -1).join('/'), {
        search: path.split('/').pop()
      })

    if (exists && exists.length > 0) {
      console.log(`⏭️  Skipped (exists): ${path}`)
      skipped++
      continue
    }

    // Upload dummy image
    const { error: uploadError } = await supabase.storage
      .from('attendance-proofs')
      .upload(path, dummyImageBuffer, {
        contentType: attendance.proof_image_mime || 'image/jpeg',
        upsert: true,
      })

    if (uploadError) {
      console.error(`❌ Failed to upload ${path}:`, uploadError.message)
      failed++
    } else {
      console.log(`✅ Uploaded: ${path}`)
      uploaded++
    }
  }

  console.log(`\n📊 Summary:`)
  console.log(`   ✅ Uploaded: ${uploaded}`)
  console.log(`   ⏭️  Skipped: ${skipped}`)
  console.log(`   ❌ Failed: ${failed}`)
  console.log(`   📁 Total: ${attendances.length}`)
}

uploadDummyImages()
  .then(() => {
    console.log('\n✨ Done!')
    process.exit(0)
  })
  .catch((error) => {
    console.error('\n💥 Error:', error)
    process.exit(1)
  })
